$.colorbox({
  html: "<%= escape_javascript render 'form' %>",
  overlayClose: false
});

dp = $('#request_date').datepicker({
    //altField: '#request_date',
    //altFormat: $.datepicker.ISO_8601,
    //defaultDate: d
});